services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_USER: brevly
      POSTGRES_PASSWORD: brevly
      POSTGRES_DB: brevly
    ports:
      - "5432:5432"
    volumes:
      - brevly-postgres:/var/lib/postgresql/data

volumes:
  brevly-postgres:

